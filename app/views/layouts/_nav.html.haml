%nav.top-bar{"data-topbar" => "", :role => "navigation"}
  %ul.title-area
    %li.name
      %h1= link_to 'Massage Scheduler', root_path
  %section.top-bar-section
    %ul.right
      - if user_signed_in?
        %li.has-dropdown
          = link_to "#{current_user.username}", account_path
          %ul.dropdown
            - if current_user.therapist?
              %li= link_to 'Create an Appointment', new_therapist_appointment_path
              %li= link_to 'Client List', client_list_path
            %li= link_to 'Appointment List', user_appointments_path(current_user)
        %li= link_to 'Sign Out', sign_out_path
      - else
        %li= link_to 'Sign In', sign_in_path
        %li= link_to 'Create An Account', sign_up_path
